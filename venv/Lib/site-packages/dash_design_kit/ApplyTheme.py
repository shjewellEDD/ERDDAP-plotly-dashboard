# AUTO GENERATED FILE - DO NOT EDIT

from dash.development.base_component import Component, _explicitize_args


class App(Component):
    """A App component.
App provides Dash Design Kit styles
and an editable theme editor.
Wrap the layout of your application with `App`
and only use one `App` component per app.

Keyword arguments:
- children (a list of or a singular dash component, string or number; optional)
- theme (dict; optional): The theme's configuration.
This configuration is editable with the theme editor.
The theme editor will also auto-generate these variables
for you in the "Copy & Save" tab.
- show_editor (boolean; optional)
- use_mobile_viewport (boolean; optional): Optimize for mobile viewports on
mobile.
If set to `False`, mobile devices will display in desktop mode.
- className (optional)
- style (dict; optional)

Available events: """
    @_explicitize_args
    def __init__(self, children=None, theme=Component.UNDEFINED, show_editor=Component.UNDEFINED, use_mobile_viewport=Component.UNDEFINED, className=Component.UNDEFINED, style=Component.UNDEFINED, **kwargs):
        self._prop_names = ['children', 'theme', 'show_editor', 'use_mobile_viewport', 'className', 'style']
        self._type = 'App'
        self._namespace = 'dash_design_kit'
        self._valid_wildcard_attributes =            []
        self.available_events = []
        self.available_properties = ['children', 'theme', 'show_editor', 'use_mobile_viewport', 'className', 'style']
        self.available_wildcard_properties =            []

        _explicit_args = kwargs.pop('_explicit_args')
        _locals = locals()
        _locals.update(kwargs)  # For wildcard attrs
        args = {k: _locals[k] for k in _explicit_args if k != 'children'}

        for k in []:
            if k not in args:
                raise TypeError(
                    'Required argument `' + k + '` was not specified.')
        super(App, self).__init__(children=children, **args)

    def __repr__(self):
        if(any(getattr(self, c, None) is not None
               for c in self._prop_names
               if c is not self._prop_names[0])
           or any(getattr(self, c, None) is not None
                  for c in self.__dict__.keys()
                  if any(c.startswith(wc_attr)
                  for wc_attr in self._valid_wildcard_attributes))):
            props_string = ', '.join([c+'='+repr(getattr(self, c, None))
                                      for c in self._prop_names
                                      if getattr(self, c, None) is not None])
            wilds_string = ', '.join([c+'='+repr(getattr(self, c, None))
                                      for c in self.__dict__.keys()
                                      if any([c.startswith(wc_attr)
                                      for wc_attr in
                                      self._valid_wildcard_attributes])])
            return ('App(' + props_string +
                   (', ' + wilds_string if wilds_string != '' else '') + ')')
        else:
            return (
                'App(' +
                repr(getattr(self, self._prop_names[0], None)) + ')')
